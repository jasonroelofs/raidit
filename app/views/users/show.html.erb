<h2>Character Info</h2>

<div class="character-info">
  <div class="main span5">
    <div class="character span4" data-character-name="<%= normalize_name @main_character.name %>">
      <%= character_icon @main_character, :large %>
      <div class="name">
        <%= @main_character.name %>
      </div>
    </div>
  </div>

  <div class="alts span5">
    <% @alt_characters.each do |character| %>
      <div class="character span4" data-character-name="<%= normalize_name character.name %>">
        <%= character_icon character, :large %>
        <div class="name">
          <%= character.name %>
        </div>
      </div>
    <% end %>
  </div>
</div>
